## Ghost kitchen internal accounting web app (Excel-matching logic): auth, CRUD, calculations, dashboards, and printable reports

To run app build and run docker image: `docker build -t app . docker run -it --network host app`
